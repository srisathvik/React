import { Outlet } from "react-router-dom";

export default function IdDetails(){
    // const id = useParams();
    // console.log(id);
    return(
        <>
            <Outlet />
        </>
    )
}